/**
 * Re-exports the ErrorBoundary class component.
 *
 * Import from this path for any new code.
 * The implementation lives in ../ErrorBoundary.tsx.
 */
export { ErrorBoundary } from '../ErrorBoundary';
